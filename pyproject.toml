[project]
name = "hamllm"
version = "0.0.1"
description = "Minimal Gmail -> Ollama mail bridge"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
hamllm = "hamllm.bridge:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
