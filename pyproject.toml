[project]
name = "hamllm"
version = "0.1.0"
description = "Local Ollama runtime and bounded integration layer"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
hamllm = "hamllm.cli:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
